let a = 0;
mut b = 0;
let f = func(a,b);
let g = func(a,func(c,d));
let nya = hoge;
let hoge = 100;
let h_oge = 100;
let prin = \(n,s){
    mut r = 0;
};

let f = \(n){
    if is_zero(n) {
        let unit = print(a);
    }
    let unit = print(n);
};

let f = \(n){
    if is_zero(n) {
        let unit = print(a);
    } else {
        let unit = print(n);
    }
};

