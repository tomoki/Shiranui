let sum = \(n){
    mut a = 0;
    for i in [0..n-1] {
        a <- !a + i;
    }
    return !a;
};

let unit = print(sum(1));
let unit = print(sum(2));
let unit = print(sum(3));
let unit = print(sum(4));
